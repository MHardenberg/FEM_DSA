#include "../src/quick.hpp"
#include <cstdlib>


int main (int argc, char *argv[]) {
    int *arr = (int*) malloc(sizeof(int)*32); // = {3, 4, 1, 2, 6, 8, 1, 0, 11, 15, 5, 7};
    quickSort(arr, 32);
    return 0;
}
